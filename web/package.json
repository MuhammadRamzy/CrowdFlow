{
  "name": "crowdflow-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "engine": "cd ../engine/cf-wasm && wasm-pack build --target web --out-dir ../../web/src/engine --no-typescript --release",
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "lint": "eslint src --max-warnings 0",
    "schema": "json2ts -i ../schema/venue.schema.json -o src/schema/venue.ts --no-additionalProperties --bannerComment \"/* eslint-disable */\n/**\n * GENERATED from schema/venue.schema.json — do not edit.\n * Regenerate with: pnpm schema\n * The source of truth is engine/cf-schema (ADR 0001).\n */\" && json2ts -i ../schema/scenario.schema.json -o src/schema/scenario.ts --no-additionalProperties --bannerComment \"/* eslint-disable */\n/**\n * GENERATED from schema/scenario.schema.json — do not edit.\n * Regenerate with: pnpm schema\n */\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.1.0",
    "@fontsource/ibm-plex-sans-condensed": "^5.1.0",
    "pixi.js": "^8.6.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zustand": "^5.0.2"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.3",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^30.0.1",
    "json-schema-to-typescript": "^15.0.4",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.10"
  }
}
